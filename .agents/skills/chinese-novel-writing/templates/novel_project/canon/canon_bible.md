# Canon Bible

`canon/` 保存原作基础事实或原创项目的基础设定。它不是剧情重构分支的草稿区。

## 规则

- 改写分支不得直接覆盖 canon。
- 推测内容必须标记为 `inferred` 或 `uncertain`。
- 用户明确指定的设定标记为 `user_override`。
- 重大变更必须先进入 `pending_updates/`。

## 基础设定

- 待填写。

